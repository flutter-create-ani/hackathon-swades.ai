# Next.js loads .env* from apps/web. Copy to .env.local for local overrides.
NEXT_PUBLIC_SERVER_URL=http://localhost:3001

# Speaker diarization is configured on the API server (ASSEMBLYAI_API_KEY in apps/server or repo root .env), not here.
